# Guitar Tabs Analysis

Generado desde [gauntlet-template](https://github.com/MrJuancho/gauntlet-template).

```bash
just setup      # uv sync + just doctor
just gauntlet   # ruff + mypy --strict + lint-imports + tests, cobertura >=90%
```

Ver `AGENTS.md` para la guía operativa completa (arquitectura, guantelete,
roles de agente) -- tiene secciones `<!-- PROJECT-SPECIFIC -->` por llenar.
